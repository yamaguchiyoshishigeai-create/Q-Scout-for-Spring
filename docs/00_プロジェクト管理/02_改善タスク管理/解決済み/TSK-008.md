# TSK-008 英語版アップロードボタンと説明系文言の整合を取る

- 区分: 多言語化
- 件名: 英語版アップロードボタンと説明系文言の整合を取る
- 状態: 解決済み
- 優先度: 中
- 実施主体: ChatGPT(リポジトリ編集) / ユーザー(人)
- 関連箇所: テンプレート、メッセージ定義、フロントエンド表示制御

## 発端
修正点メモ、外部評価反映改善タスク

## 現状認識
送信ボタン文言はアプリ側 i18n により英語化済みである一方、従来のファイル選択欄はブラウザ標準UI表示に依存していたため、`?lang=en` でもブラウザ / OS のロケールによって「ファイルを選択」「選択されていません」と表示されることがあった。

このため、ファイル選択UIをアプリ側で制御するカスタム表示へ変更し、英語ページでは `Choose File` / `No file selected`、日本語ページでは `ファイルを選択` / `選択されていません` が表示されるようにした。

さらに、ファイル選択後も未選択表示のまま残る問題に対し、ファイル入力の変更時に表示要素を直接更新する処理と、`home.js` 側の選択ファイル取得処理を堅牢化した。

説明系文言の機械的なキー整合確認は、後続の `TSK-039 i18nキー整合チェッカーを実装する` に委ねる。

## 対応方針
主要画面と help の英日整合を横断確認する。

1. ChatGPT(リポジトリ編集) でテンプレートとメッセージ定義を確認する。
2. ファイル選択UIをブラウザ標準表示依存から、アプリ制御のカスタム表示へ切り替える。
3. 実画面確認、スクリーンショット確認、テスト実行を行い、問題がなければ解決済み化する。

## 完了条件
日本語・英語双方で主要導線が自然に読める。

特に英語ページで、ファイル選択欄が `Choose File` / `No file selected` として表示されること、ファイル選択後に選択ファイル名へ更新されることを確認する。

## 対応内容
- PR #76 により、トップページのファイル選択欄をブラウザ標準UI依存からカスタム表示へ変更した。
- 英語ページで `Choose File` / `No file selected` を表示するようにした。
- 日本語ページで `ファイルを選択` / `選択されていません` を表示するようにした。
- ファイル選択後に選択ファイル名を表示するようにした。
- 20MB超過時には選択状態と表示をリセットするようにした。
- ユーザー目視確認により、狙い通りの修正であることを確認した。

## 関連ファイル
- `src/main/resources/templates/index.html`
- `src/main/resources/static/js/home.js`
- `src/main/resources/messages_en.properties`
- `src/main/resources/messages_ja.properties`

## 関連PR
- PR #76: TSK-008-B カスタムファイル選択UIによる英語表示統一

## 備考
TSK-008固有の残件はなし。i18nキーの機械的な整合確認は別タスク `TSK-039` として継続管理する。
