# TSK-003 結果サマリ文言の誇大表現を是正する

- 区分: UI / UX
- 件名: 結果サマリ文言の誇大表現を是正する
- 状態: 解決済み
- 優先度: 中
- 実施主体: ChatGPT(会話内) → ChatGPT(リポジトリ編集)
- 関連箇所: `src/main/resources/templates/index.html`, `src/main/resources/templates/fragments/summary.html`

## 発端
修正点メモ

## 現状認識
- 変更前の文言は `解析後はここで対象ファイル、実行時刻、総合スコア、重大度内訳をまとめて確認できます。` であった。
- 「解析後はここで」という表現が表示位置依存かつ説明調に見えやすく、「確認できます」も実態より断定的に受け取られる可能性があった。

## 対応方針
- 文言を `解析対象ファイル、実行開始時刻、総合スコア、重大度内訳を下記にまとめます。` へ変更する。
- あわせて、「対象ファイル」から「解析対象ファイル」、「実行時刻」から「実行開始時刻」へ置換し、表示内容と粒度を明確化する。
- トップページ本体と結果サマリ断片のフォールバック文言を揃える。

## 完了条件
- `解析後はここで対象ファイル、実行時刻、総合スコア、重大度内訳をまとめて確認できます。` が `解析対象ファイル、実行開始時刻、総合スコア、重大度内訳を下記にまとめます。` へ置換されている。
- 初見利用者に過剰な期待を与えない説明になっている。

## 関連ファイル
- `src/main/resources/templates/index.html`
- `src/main/resources/templates/fragments/summary.html`

## 関連PR
- PR作成後に追記予定

## 備考
- 2026-04-24: 実装側の文言置換を反映し、解決済みへ移動。
- `page.home.section.summary.copy` は多言語メッセージとしても使用されているため、日本語表示では対象箇所に固定済み文言を適用し、英語表示では既存英語文言を維持する。
