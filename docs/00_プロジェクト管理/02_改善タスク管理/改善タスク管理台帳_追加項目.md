# 改善タスク管理台帳 追加項目

## 目的

本書は、`改善タスク管理台帳.md` に対して安全に統合する前段として、追加確定した改善項目を参照付きで保持するための補助台帳である。

既存台帳本体の運用ルール、状態定義、既存タスク一覧は `改善タスク管理台帳.md` を正本とし、本書はその補助資料として扱う。

## 追加項目一覧

| 管理ID | 区分 | 件名 | 状態 | 優先度 | 実施主体 | 関連箇所 | 概要 | 完了条件 | 根拠 / 関連PR / 備考 |
|---|---|---|---|---|---|---|---|---|---|
| TSK-015 | 公開サンプル評価 | `spring-boot-realworld-example-app` の保留解除可否を個別検証する | 保留 | 中 | Codex | 公開 sample 評価、Gradle build、Q-Scout CLI 入力前提 | 実務寄り API 構成として有望だが、現時点では Compile=FAIL / Q-Scout=FAIL のため、build 前提差異と CLI 入力前提差異を分けて個別検証する必要がある。 | build 前提差異と Q-Scout 入力前提差異が整理され、保留解除 / 継続保留 / 改善タスク化のいずれかへ再判定できる。 | `docs/00_プロジェクト管理/02_改善タスク管理/公開サンプル個別検証_spring-boot-realworld-example-app.md` を参照。 |

## 追加項目詳細

### TSK-015
- 区分: 公開サンプル評価
- 件名: `spring-boot-realworld-example-app` の保留解除可否を個別検証する
- 状態: 保留
- 優先度: 中
- 実施主体: Codex
- 関連箇所: 公開 sample 評価、Gradle build、Q-Scout CLI 入力前提
- 発端: 公開 sample 10件評価の結果整理
- 現状認識:
  - `samples/sample-comparison-summary.md` では Clone=OK, Compile=FAIL, Q-Scout=FAIL
  - `samples/CodexExec.result` では Gradle 7.4 実行時の `Unsupported class file major version 65` と、`run-cli.bat` 実行時の `pom.xml was not found` が主要失敗要因として記録されている
  - 現時点の正式分類では保留候補である
- 対応方針:
  1. build 前提差異を切り分ける
  2. Q-Scout 側の入力前提差異を切り分ける
  3. その結果に応じて、保留解除 / 継続保留 / Q-Scout 側改善タスク化を再判定する
- 完了条件:
  - build 前提差異が整理されている
  - Q-Scout 入力前提差異が整理されている
  - 保留解除条件の可否が判断できる
- 関連ファイル:
  - `docs/00_プロジェクト管理/02_改善タスク管理/公開サンプル個別検証_spring-boot-realworld-example-app.md`
  - `samples/CodexExec.result`
  - `samples/sample-comparison-summary.md`
- 関連PR: なし
- 備考:
  - 当面の正式分類は保留候補のままとする
  - 中核候補群と補助候補群の初期運用を優先し、その後必要時に再開する
